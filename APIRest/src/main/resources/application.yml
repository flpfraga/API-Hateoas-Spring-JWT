cors:
  originPatterns: https://www.imdb.com/

security:
  jwt:
    token:
      secret-key: 53cr37
      expire-length: 3600000

server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ioasys-api?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    
flyway:
  url: jdbc:mysql://localhost:3306/
  schemas: api-hateoas
  user: root
  password: admin123
  
spring-doc:
  pathsToMatch: /api/**/v1/**
  swagger-ui:
    use-root-path: true